<?xml version="1.0" encoding="utf-8"?>

<!-- Using ConstraintLayout as parent ViewGroup-->
<!-- Too complex compared to RelativeLayout -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/text_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="36sp"
        android:textStyle="bold"
        android:layout_marginTop="64dp"
        android:layout_marginBottom="32dp"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/textLatestImc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="28sp"
        android:layout_marginTop="32dp"
        android:visibility="gone"
        android:layout_below="@id/text_title"
        android:layout_centerHorizontal="true"
        app:layout_constraintTop_toBottomOf="@id/text_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible"
        tools:text="Recente: 38.4"/>

    <EditText
        tools:visibility="visible"
        android:id="@+id/edit_height"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="300dp"
        android:hint="height (m)"
        android:imeOptions="actionNext"
        style="@style/input_style"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/textLatestImc"
        app:layout_constraintTop_toBottomOf="@id/textLatestImc"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <EditText
        tools:visibility="visible"
        android:id="@+id/edit_weight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="weight (Kg)"
        android:minWidth="300dp"
        android:imeOptions="actionDone"
        style="@style/input_style"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/edit_height"
        app:layout_constraintTop_toBottomOf="@id/edit_height"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <Button
        tools:visibility="visible"
        android:id="@+id/button_calculate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint=""
        android:text="@string/calculate"
        android:textSize="18sp"
        android:padding="16dp"
        android:layout_marginTop="36dp"
        android:layout_gravity="center"
        android:minWidth="300dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/edit_weight"
        app:layout_constraintTop_toBottomOf="@id/edit_weight"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <TextView
        android:id="@+id/text_response"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="64dp"
        android:textSize="24sp"
        android:textAllCaps="true"
        android:visibility="gone"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/button_calculate"
        tools:text="Você está bem na fita"
        app:layout_constraintTop_toBottomOf="@id/button_calculate"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible"
        />

</androidx.constraintlayout.widget.ConstraintLayout>